---
title: Home
---

import { graphql } from "gatsby"
import { Grid, Box } from "theme-ui"
import Item from "../components/item-small"
import { PostListA1 } from "../components/postlist"
import Carousel from "../components/carousel"


<Carousel />

<Box sx={{ maxWidth: "blog", mx: "auto", textAlign: "center", mt: 5 }}>

# [Últimos libros](/libros)

Nemo enim ipsam voluptatem quia voluptas sit aspernatur aut odit aut fugit, sed quia consequuntur magni dolores eos qui ratione voluptatem [sequi nesciunt](/libros).

</Box>
<Box sx={{ maxWidth: "blog", mx: "auto", px: 1,pt:4 }}>

<Grid columns={[1, 3]} variant="primary" sx={{ mb: 4 }}>
  {props.data.allContentfulLibro.nodes.slice(2).map(node => (
    <Item key={node.id} data={node} />
  ))}
</Grid>
</Box>

<Box sx={{ maxWidth: "blog", mx: "auto", textAlign: "center", mt: 5 }}>

# [Blog](/blog)

Nemo enim ipsam voluptatem quia voluptas sit aspernatur aut odit aut fugit, sed quia consequuntur magni dolores eos qui ratione voluptatem [sequi nesciunt](/blog).

</Box>

<PostListA1 posts={props.data.allContentfulBlogPost.nodes} />

<Box sx={{ maxWidth: "blog", mx: "auto", display: "flex" }}>

</Box>

export const pageQuery = graphql`
  query HomePageQuery {
    allContentfulLibro(limit: 5, sort: { order: DESC, fields: createdAt }) {
      nodes {
        contentful_id
        id
        title
        authors
        price
        image {
          gatsbyImageData(width: 480)
        }
        categories
        collections
      }
    }
    allContentfulBlogPost(limit: 3, sort: { order: DESC, fields: date }) {
      nodes {
        id
        title
        date(formatString: "D MMMM YYYY", locale: "es")
        description
      }
    }
  }
`
